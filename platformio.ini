; ----------------------------------------------------------------------------
; claude-desktop-buddy — Xteink X4 firmware. FreeInk SDK (e-paper), vendored
; as the freeink-sdk git submodule at the project root (see .gitmodules;
; `git submodule update --init` before building). Mirrors freeink-sdk's own
; platformio.sample.ini [base] + [env:xteink_x4] pattern; see README.md for
; the sprite region / refresh timer / input-mapping decisions.
; ----------------------------------------------------------------------------
[env:xteink_x4]
platform = https://github.com/pioarduino/platform-espressif32/releases/download/55.03.37/platform-espressif32.zip
board = esp32-c3-devkitm-1
framework = arduino
monitor_speed = 115200
upload_speed = 921600
board_build.flash_mode = dio
board_build.flash_size = 16MB
; esp32-c3-devkitm-1's stock partition table targets its 4MB default flash
; (a ~1.3MB app slot) — too small for this firmware; the X4 is a real 16MB
; part (see platformio.sample.ini), so use the matching scheme.
board_build.partitions = default_16MB.csv
build_unflags =
    -std=gnu++11
build_flags =
    -std=gnu++2a
    -DARDUINO_USB_MODE=1
    -DARDUINO_USB_CDC_ON_BOOT=1
    ; Single framebuffer (controller RAM holds the previous frame) — same
    ; default freeink-sdk's own sample env uses for the C3's tight RAM.
    -DEINK_DISPLAY_SINGLE_BUFFER_MODE=1
    -DFREEINK_DEVICE_X4=1
    -DCORE_DEBUG_LEVEL=0
build_src_filter = +<*>
lib_deps =
    bblanchon/ArduinoJson @ ^7.0.0
    BoardConfig=symlink://freeink-sdk/libs/hardware/BoardConfig
    EInkDisplay=symlink://freeink-sdk/libs/display/FreeInkDisplay
    InputManager=symlink://freeink-sdk/libs/hardware/InputManager
    BatteryMonitor=symlink://freeink-sdk/libs/hardware/BatteryMonitor
    FreeInkUI=symlink://freeink-sdk/libs/ui/FreeInkUI
    Icons=symlink://freeink-sdk/libs/assets/Icons
    ; SD-backed character packs (see README "SD-backed character packs").
    ; X4 uses plain SPI/SdFat (FREEINK_SD_SDMMC excludes it — only
    ; de-link/X4Pro/PaperMono/MurphyM4 need the native SDMMC block device),
    ; so no USE_BLOCK_DEVICE_INTERFACE flag is needed here. SdFat itself is
    ; pulled in automatically as SDCardManager's own dependency.
    SDCardManager=symlink://freeink-sdk/libs/hardware/SDCardManager
    ; PowerManager::deepSleepUntilPowerButton() backs the menu's "turn off" —
    ; this board has no PMIC hard-off (M5's M5.Axp.PowerOff()), so real ESP32
    ; deep sleep woken by the POWER button is the equivalent off state.
    PowerManager=symlink://freeink-sdk/libs/hardware/PowerManager
