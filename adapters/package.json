{
  "name": "hardware-buddy-adapters",
  "private": true,
  "version": "0.1.0",
  "description": "Host-side bridges that let AI coding tools other than Claude's desktop apps drive a Hardware Buddy device over the wire protocol in ../REFERENCE.md.",
  "license": "MIT",
  "dependencies": {
    "serialport": "^12.0.0"
  }
}
